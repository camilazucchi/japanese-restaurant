import styles from './Menu.module.scss';

export default function Menu() {
    return (
        <main>
            <nav className={styles.menu}>
                Japanese Restaurant
            </nav>
        </main>
    )
}